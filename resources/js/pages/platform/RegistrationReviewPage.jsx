import { useEffect,useState } from 'react'
import { useApp } from '../../context/AppContext'
import { errorMessage } from '../../services/api'

export default function RegistrationReviewPage(){const{api}=useApp();const[items,setItems]=useState([]);const[notice,setNotice]=useState('');const load=()=>api.get('/admin/course-registrations').then(r=>setItems(r.data?.data||[]));useEffect(()=>{load().catch(e=>setNotice(errorMessage(e)))},[api]);const review=async(id,status)=>{try{await api.patch(`/admin/course-registrations/${id}`,{status});setNotice('تم حفظ قرار المراجعة.');load()}catch(e){setNotice(errorMessage(e))}};return <main className="meh-workflow" dir="rtl"><section className="misr-dashboard-hero"><div><span className="p-eyebrow">إدارة التسجيلات</span><h1>مراجعة إثباتات الدفع</h1><p>الموافقة تمنح الطالب الوصول للدورة باستخدام نظام التسجيل الحالي.</p></div></section>{notice&&<p className="p-notice">{notice}</p>}<div style={{overflowX:'auto'}}><table className="meh-admin-table"><thead><tr><th>الطالب</th><th>الدورة</th><th>المبلغ</th><th>الحالة</th><th>الإثبات</th><th>الإجراء</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.student_name}<small style={{display:'block'}}>{x.student_email}</small></td><td>{x.course_title}</td><td>{x.amount_lyd} د.ل</td><td><span className="meh-status">{x.status}</span></td><td>{x.proof_name||'لم يرفع'}</td><td><div className="meh-admin-actions"><button className="btn btn-primary" onClick={()=>review(x.id,'approved')}>موافقة</button><button className="btn btn-ghost" onClick={()=>review(x.id,'rejected')}>رفض</button></div></td></tr>)}</tbody></table></div>{!items.length&&<div className="p-empty">لا توجد تسجيلات للمراجعة.</div>}</main>}
