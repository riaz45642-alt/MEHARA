import { useEffect,useState } from 'react'
import { useApp } from '../../context/AppContext'
import { errorMessage } from '../../services/api'

export default function NewsPage(){const{api,language}=useApp();const[items,setItems]=useState([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');const t=(ar,en)=>language==='ar'?ar:en;const load=()=>{setLoading(true);api.get('/public/news',{params:{q:q||undefined}}).then(r=>setItems(r.data?.data||[])).catch(e=>setError(errorMessage(e))).finally(()=>setLoading(false))};useEffect(load,[]);return <main className="p-page"><section className="p-page-hero"><div className="wrap"><span className="p-eyebrow">MEHARA SIGNAL</span><h1>{t('آخر الأخبار والتقنيات','Latest news & technology')}</h1><p>{t('تحديثات موثوقة حول التعلم الرقمي، المهارات والتقنيات التي تشكل مستقبل العمل.','Reliable updates on digital learning, skills and technologies shaping the future of work.')}</p><form className="p-search" onSubmit={e=>{e.preventDefault();load()}}><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder={t('ابحث في الأخبار','Search news and updates')}/><button>{t('بحث','Search')}</button></form></div></section><section className="p-section wrap"><div className="news-grid">{loading?<div className="p-empty">{t('جارٍ تحميل التحديثات...','Loading updates…')}</div>:items.map((x,i)=><article className={i===0&&x.is_featured?'featured':''} key={x.id}>{x.cover_image&&<img src={x.cover_image} alt=""/>}<div><span className="p-eyebrow">{x.category} · {new Date(x.published_at||x.created_at).toLocaleDateString(language)}</span><h2>{x.title}</h2><p>{x.excerpt||x.body.slice(0,220)}</p><small>{t('بقلم','By')} {x.author}</small></div></article>)}{!loading&&!items.length&&<div className="p-empty"><strong>{t('لا توجد أخبار منشورة بعد.','No published updates yet.')}</strong><p>{t('ستظهر هنا التحديثات التي يعتمدها فريق مِهارة.','Updates approved by the MEHARA team will appear here.')}</p></div>}</div>{error&&<p className="p-notice">{error}</p>}</section></main>}
